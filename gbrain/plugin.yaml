name: gbrain
version: 0.1.0
description: "GBrain — local SQLite/FTS5 note store with deterministic entity extraction, alias linking, and prefetch recall. Inspired by garrytan/gbrain, no network or model calls."
hooks:
  - on_memory_write
